def go(parsing_res): # This function will return True or False, True == everything is ok,
                     # False == there is an error, it has been printed on the
                     # console ( Stop starting modules and just quit() if False)
    pass
